<views:GlassWindow x:Class="StockWidget.App.Views.AmountTrendWindow"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:views="clr-namespace:StockWidget.App.Views"
                   Title="沪深成交额趋势"
                   Width="640" Height="420"
                   WindowStartupLocation="CenterOwner"
                   ResizeMode="NoResize" ShowInTaskbar="False"
                   Background="{DynamicResource BgBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <TextBlock Text="📈 沪深成交额趋势（近 60 个交易日）" FontSize="14" FontWeight="Bold"
                       Foreground="{DynamicResource FgBrush}" />
            <Button Content="✕ 关闭" Width="80" Height="26" HorizontalAlignment="Right"
                    Click="Close_Click" VerticalAlignment="Center" />
        </Grid>

        <TextBlock Grid.Row="1" x:Name="StatsText" Margin="0,4,0,0" FontSize="11"
                   TextWrapping="Wrap" Foreground="{DynamicResource SubFgBrush}" />

        <Border Grid.Row="2" Background="{DynamicResource CardBrush}" CornerRadius="10" Margin="0,10,0,0">
            <views:AmountTrendChart x:Name="Chart" Margin="10" />
        </Border>
    </Grid>
</views:GlassWindow>
