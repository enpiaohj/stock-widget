# Changelog

所有对外可感知的变更记录于此。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- 日K线自动归档：每个交易日 15:05 后首个刷新自动落库全自选 OHLC（`daily_kline` 表，零额外请求、幂等可补档）
- K线图：双击行打开的分时窗口新增「分时 / 日K」页签——蜡烛图 + 成交量副图 + MA5/10/20 均线 + 近 5/20/60/250 日涨跌标签
- 成交额趋势窗口：右键菜单「📈 成交额趋势」或双击底部量能栏打开——近 60 日柱状（放量红/缩量绿）+ 5/20 日均线 + 统计（较昨增减、均值、60 日排名）

## [0.1.2] - 2026-09-16

### Fixed

- 修复启动后设置不生效的间歇性无响应：走势快照查询移出 UI 线程（此前每 5 秒刷新在 UI 线程逐行同步查询
  SQLite 并与后台写入抢锁，导致透明度/主题切换/调整顺序等操作周期性卡顿失效）
- 调整顺序改为物理移动行：修复"当时不移动、重开应用才生效"（视图排序器使用旧 SortOrder 吞掉移动）
- "移至顶部"功能修复：目标位原被固定股占据导致永远失败，现落到第一个非固定股位置
- 关闭表头单击排序（与手动调整顺序互相干扰），保留双击表头隐藏窗口
- 窗口阴影容错加载：无 GPU / 远程会话环境静默回退，保证启动不失败（0.1.1 补丁的后续加固）
- 深色模式表头文字不可见（表头样式未继承主题配色）
- 行信息提示改为鼠标悬停显示（行原生 ToolTip，消除闪烁与右键干扰）
- 主窗口高度按内容收紧，消除底部大片空白
- 表头/字段样式列间距优化，走势列加宽并左留间距

### Added

- 表格列全字段显示恢复（窗口按列宽合计自适应，超屏时表格内部滚动兜底）
- 数据备份导入导出：设置中心 → 通用 → 导出/导入 JSON 备份（设置 / 自选股 / 历史成交额 / 预警规则，导入合并去重）
- 数字输入控件（SpinnerBox）：设置中字号 / 刷新间隔 / 透明度 / 列宽支持 ▲▼ 长按连发、滚轮与 ↑↓ 键步进
- 设置中心可选强调色（橙 / 蓝 / 绿 / 紫 / 红 / 青）整套联动，取消设置自动恢复预览
- 整行红绿着色（全部字段按涨跌幅染色，对齐旧版）+ 行间虚线分隔线设置生效
- 主窗口锁定状态徽章；分时图昨收基准虚线；主题切换提速

### Changed

- 透明度改为 10–100 刻度（所见即所得：100=完全不透），仅背景透视、文字恒清晰
- 窗口拖动改为位移判别：按住表格行 / 表头 / 空白区移动即拖动窗口，轻点仍选择行、双击仍打开分时图
- 设置窗口套与主窗口一致的渐变卡片视觉；二级菜单（调整顺序 / 窗口设置）可正常展开
- 崩溃日志超 5MB 自动轮转，防止循环异常刷爆磁盘

## [0.1.1] - 2026-09-15

### Fixed

- 修复启动崩溃：主窗口 `DropShadowEffect` 的 `Color` 属性引用了 `SolidColorBrush` 资源导致 `XamlParseException`（设置 `UIElement.Effect` 异常，应用无法启动）
- 窗口阴影改为代码容错加载：无 GPU / 远程会话 / 软件渲染环境下 `DropShadowEffect` 创建失败时静默回退为无阴影细边框，保证应用始终正常启动与渲染

## [0.1.0] - 2026-09-15

首个 C# / WPF 重写正式版本（含基线功能与 v1.1 完善）。

### Added

- C# / WPF (.NET 10) 全新重写，功能对齐 Python 旧版（PySide6 UI v1.0.3.10）并全面增强
- 本地 SQLite 数据库（EF Core 10，Code-First 迁移）：设置、自选股、每日成交额历史（自动清理保留 2 年）、
  当日行情快照、预警规则六表（含交易日历）+ 迷你走势；库文件便携优先（`<exe目录>\data\`），无写权限自动回退 `%APPDATA%\StockWidget\`
- 旧版 Python 数据迁移：自动扫描 + 设置中心手动导入 `stock_config.json` / `history_amount.json`
  （自选股、全部界面设置、列宽、历史成交额完整迁移，原文件保留不删）
- 玻璃拟态 UI：无边框圆角 + 渐入投影、深 / 浅 / 跟随系统三态主题热切换、值变动行闪烁与现价 ↑/↓ 箭头动效、
  可选中强调色（橙/蓝/绿/紫/红/青）整套联动、主窗口锁定状态徽章、窗口显隐淡入淡出
- 行情表增强：表头单击排序（升 / 降 / 取消循环，含 ▲/▼ 指示）、指数 / ETF / 个股自动分组（左竖条分组头）、
  键盘 Delete 删除选中、量能金额等宽数字（Tabular Figures）
- 分时迷你走势：行内 Sparkline（当日快照，面积渐变 + 末点光标）+ 双击行打开当日分时图（腾讯分时接口，昨收基准虚线）
- 桌面体验包：个股涨跌幅阈值托盘通知预警（同日同规则去重）、开机自启动（注册表可开关）、
  单实例互斥、窗口越界自动拉回
- 全局热键改用 Win32 `RegisterHotKey`（更稳更省电），支持捕获式录入任意组合
- 节假日休市识别：内置交易日历（`trading_calendar` 表，2025–2026 已确定年份）+ 休市智能抓取
  （休市日跳过抓取、拉长到 600s 探测间隔，恢复交易日自动切回配置间隔）
- 按名称/拼音添加股票：腾讯 smartbox 搜索下拉联动（防抖 300ms、键盘/鼠标选择）
- `ShowDividers` 行间分隔线设置真正生效（旧版为空置配置）
- 单元测试 64 个（代码归一化 / 腾讯响应解析 / 仓储 / 设置 / 热键解析 / 交易日历 / smartbox 解析 / 强调色板）

### Changed

- 行情抓取由"每股票一个线程"改为单请求批量抓取 + 失败保留旧值
- 底部量能栏改为富文本（总量能 / 较上一交易日增量缩量 / 可点击状态段）
- 状态栏周数显示默认关闭（对齐旧版运行时实际行为，消除旧版默认值不一致缺陷）

### Fixed

- 接通托盘图标大盘涨跌色点（刷新后红涨绿跌实时更新），修复基线该功能名存实亡
- 节假日休市无法识别（新增交易日历识别，见 Added）
- 删除模板残留空类 `Class1.cs`
- 旧版"取消设置未恢复主题"缺陷（本次同步恢复强调色预览）
- 旧版 `show_update_week_number` / `font_size` 默认值不一致问题

### Known Issues

- 交易日历内置至 2026 已确定年份；2026 部分节假日后缀调休、及 2027 后的调休上班周末按"周末休市"兜底，
  待官方安排公布后增量补充种子（可持续维护）
- 行情数据源为腾讯公开接口，接口字段变更需同步更新解析器

### Verification

- Build：Debug / Release 均通过（0 警告 0 错误）
- Tests：64 / 64 通过
- Runtime：Debug 与单文件发布版（win-x64 自包含）冒烟——正常启动、自动建库 / 迁移 / 种子、
  行情抓取与名称回填、强调色切换、休市徽章与智能抓取